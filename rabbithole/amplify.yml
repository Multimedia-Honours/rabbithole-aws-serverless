version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - npm ci --force
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: dist/rabbithole
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    appRoot: rabbithole
