version: 1
applications:
  - appRoot: rabbithole
    # backend:
    #   phases:
    #     build:
    #       commands:
    #         - '# Backend Deploying : for now we do not need auto deployment for backend (Except when we add testing)'
    #         - amplifyPush --simple
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: dist/rabbithole
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
